#pragma once

#include "stdafx.h"
#include "ILesson.h"
#include "Utils.h"

namespace rescan
{
	class Lesson31 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson31(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson32 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson32(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson33 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson33(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson34 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson34(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson35 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson35(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson36 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson36(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson37 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson37(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson38 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson38(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson39 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson39(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};

	class Lesson40 :
		public ILesson
	{
	public:
		// Constructor.
		Lesson40(Rect rect, Graphics2D* graphics, Keyboard* kbd, Mouse* mouse, TextToSpeech* tts);

	private:
		void SpeakCurrentItem();
		void LoadLessons();
	};
}